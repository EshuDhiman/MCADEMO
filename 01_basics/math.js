const num=-10000
console.log(Math.abs(num));
console.log(Math.random()); // will always give value b/w 0-1 so multiply by 10
console.log((Math.random()*10)+1); // will always give value b/w 0-1 so multiply by 10