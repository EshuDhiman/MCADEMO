const name = "Eshu"
const age = 18
console.log(`name is ${name} and age is ${age}`);
const game = new String('valorant')
console.log(game.__proto__);
console.table([game.length,game.toLocaleUpperCase()]);
